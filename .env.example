# HuggingFace authentication (for gated models)
HF_TOKEN=

# Number of text samples per language (default: 1000)
NSANKU_TTS_NUM_SAMPLES=1000

# Alignment backend: python (default) or ctc_forced_aligner
ALIGNMENT_BACKEND=python
